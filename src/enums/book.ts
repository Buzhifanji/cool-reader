/**
 * 书本扩展名，目前所支持解析的书本类型
 */
export enum Bookextname {
  pdf = "pdf",
  epub = "epub",
}

export enum NotesType {
  notes,
  highlight,
}

// 阅读工具栏选项
export enum barEnum {
  Copy,
  TextHighlight,
  tilde,
  straightLine,
  edit,
  idea,
}